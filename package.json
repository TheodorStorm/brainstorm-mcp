{
  "name": "brainstorm",
  "version": "1.1.0",
  "description": "MCP server enabling multi-agent collaboration and coordination",
  "type": "module",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "node scripts/generate-version.js && tsc",
    "dev": "tsc --watch",
    "start": "node dist/src/index.js",
    "lint": "eslint src",
    "config": "npm install && npm run build && node scripts/configure-mcp.js",
    "test": "npm install && npm run build && node --test dist/tests/*.test.js",
    "cleanup": "node scripts/cleanup-project.js"
  },
  "keywords": [
    "mcp",
    "agent",
    "cooperation",
    "collaboration"
  ],
  "author": "Theodor Storm",
  "license": "BUSL-1.1",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.6.0",
    "borgmcp": "^0.2.0-beta.4"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
